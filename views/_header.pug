header.header
  nav.nav.nav--animes
    a.nav__el(href='/') Accueil
  .header__logo
    img(src='/img/logo-white.png' alt='Natours logo')
  nav.nav.nav--user
    if user
      a.nav__el.nav__el--logout Déconexion
      a.nav__el(href='/me')
        img.nav__user-img(src=`/img/users/${user.photo}` alt=`Photo de ${user.name}`)
        span= user.name.split(' ')[0]
    else
      a.nav__el(href='/login') Connexion
      a.nav__el.nav__el--cta(href='/signup') Inscription